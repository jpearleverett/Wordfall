{
  "name": "wordfall-cloud-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Wordfall club goals, push notifications, and streak reminders",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "expo-server-sdk": "^3.10.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
